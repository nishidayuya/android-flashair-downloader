# frozen_string_literal: true

# Ruby is only used by tools/flashair-stub.rb, the stand-in card the app is
# tested against. The Android app itself has no Ruby dependencies.

source "https://rubygems.org"

gem "puma", "~> 8.0"
gem "rackup", "~> 2.3"
gem "sinatra", "~> 4.2"
