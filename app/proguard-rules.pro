# OkHttp ships its own consumer rules; nothing extra is needed for it here.
#
# Room and Hilt generate code that R8 keeps via the rules bundled in their
# artifacts. Add app-specific keep rules below as they become necessary.
